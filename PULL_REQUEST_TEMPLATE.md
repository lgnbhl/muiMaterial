# NA

## Summary

## Checklist

`devtools::document()` has been run

`devtools::test()` passes

`devtools::check()` is clean (or only the expected installed-size NOTE)

`NEWS.md` updated under the unreleased section (if user-visible)

If JS sources changed: bundle rebuilt with `yarn build` and
`inst/www/muiMaterial/mui-material.js{,.LICENSE.txt}` committed
