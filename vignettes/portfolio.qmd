---
title: "Portfolio"
format:
  html:
    minimal: true
---

```{r}
#| echo: false
library(muiMaterial)
library(reactRouter)

strenght_section <- Box(
  # My Strengths section
  Box(
    sx = list(
      width = "100%",
      maxWidth = "800px",
      marginTop = 4
    ),

    Typography(
      "My strengths",
      variant = "h4",
      sx = list(
        color = "#1C1917",
        fontWeight = "bold",
        marginBottom = 3
      )
    ),

    Typography(
      "Add your key strengths and skills here...",
      variant = "body1",
      sx = list(
        color = "#292524",
        lineHeight = 1.8
      )
    )
  )
)

# Home page content
home_page <- Box(
  sx = list(
    display = "flex",
    flexDirection = "column",
    justifyContent = "center",
    alignItems = "center",
    textAlign = "center",
    padding = 4,
    minHeight = "calc(100vh - 64px)"
  ),

  Typography(
    "Hi, I'm",
    variant = "h5",
    sx = list(
      color = "#292524",
      marginBottom = 2
    )
  ),

  Typography(
    "MY NAME",
    variant = "h1",
    sx = list(
      color = "#1C1917",
      fontWeight = "bold",
      fontSize = list(
        xs = "3rem",
        sm = "4rem",
        md = "5rem"
      ),
      marginBottom = 2
    )
  ),

  Typography(
    "7 years of experience",
    variant = "h6",
    sx = list(
      color = "#6B4423",
      marginBottom = 4
    )
  ),

  # Action buttons
  Box(
    sx = list(
      display = "flex",
      gap = 2,
      justifyContent = "center",
      flexWrap = "wrap",
      marginBottom = 6
    ),
    Button(
      "View my resume",
      variant = "contained",
      sx = list(
        backgroundColor = "#6B4423",
        color = "#FAF6F1",
        textTransform = "none",
        borderRadius = "24px",
        paddingX = 3,
        paddingY = 1.5,
        `&:hover` = list(
          backgroundColor = "#5A3A1E"
        )
      )
    ),
    Button(
      "Get in touch",
      variant = "outlined",
      sx = list(
        borderColor = "#6B4423",
        color = "#6B4423",
        textTransform = "none",
        borderRadius = "24px",
        paddingX = 3,
        paddingY = 1.5,
        `&:hover` = list(
          borderColor = "#5A3A1E",
          backgroundColor = "rgba(107, 68, 35, 0.04)"
        )
      )
    )
  )
)

# Resume page content
resume_page <- Box(
  sx = list(
    padding = 4
  ),

  Typography(
    "Resume",
    variant = "h2",
    sx = list(
      color = "#1C1917",
      fontWeight = "bold",
      marginBottom = 4
    )
  ),

  Typography(
    "Work Experience",
    variant = "h4",
    sx = list(
      color = "#6B4423",
      marginBottom = 2
    )
  ),

  Typography(
    "Your work experience details will go here...",
    variant = "body1",
    sx = list(
      color = "#292524",
      marginBottom = 4
    )
  ),

  Typography(
    "Education",
    variant = "h4",
    sx = list(
      color = "#6B4423",
      marginBottom = 2
    )
  ),

  Typography(
    "Your education details will go here...",
    variant = "body1",
    sx = list(
      color = "#292524"
    )
  )
)

muiMaterialPage(
  theme = list(
    palette = list(
      mode = "light",
      primary = list(
        main = "#6B4423"
      ),
      background = list(
        default = "#FAF6F1",
        paper = "#FAF6F1"
      ),
      text = list(
        primary = "#1C1917",
        secondary = "#292524"
      )
    ),
    typography = list(
      fontFamily = '"Inter", "Roboto", "Helvetica", "Arial", sans-serif'
    )
  ),
  reactRouter::HashRouter(
    Box(
      sx = list(
        minHeight = "100vh",
        backgroundColor = "#FAF6F1",
        display = "flex",
        flexDirection = "column"
      ),

      # Navbar
      AppBar(
        position = "static",
        elevation = 0,
        sx = list(
          backgroundColor = "#FAF6F1",
          color = "#1C1917",
          borderBottom = "1px solid rgba(0, 0, 0, 0.12)"
        ),
        Toolbar(
          # Left side buttons
          Box(
            sx = list(
              display = "flex",
              gap = 2,
              flexGrow = 1
            ),
            reactRouter::NavLink(
              to = "/",
              style = JS(
                '({isActive}) => { return {textDecoration: "none"}; }'
              ),
              Button(
                "Home",
                sx = list(
                  color = "#1C1917",
                  textTransform = "none",
                  fontSize = "1rem"
                )
              )
            ),
            reactRouter::NavLink(
              to = "/resume",
              style = JS(
                '({isActive}) => { return {textDecoration: "none"}; }'
              ),
              Button(
                "Resume",
                sx = list(
                  color = "#1C1917",
                  textTransform = "none",
                  fontSize = "1rem"
                )
              )
            )
          ),

          # Right side buttons
          Box(
            sx = list(
              display = "flex",
              gap = 2
            ),
            Button(
              "GitHub",
              sx = list(
                color = "#1C1917",
                textTransform = "none",
                fontSize = "1rem"
              )
            ),
            Button(
              "LinkedIn",
              sx = list(
                color = "#1C1917",
                textTransform = "none",
                fontSize = "1rem"
              )
            )
          )
        )
      ),

      # Routes
      reactRouter::Routes(
        reactRouter::Route(
          path = "/",
          element = home_page
        ),
        reactRouter::Route(
          path = "/resume",
          element = resume_page
        ),
        reactRouter::Route(
          path = "*",
          element = Box(
            Typography(
              "Error 404 - Page not found",
              variant = "h4",
              sx = list(
                textAlign = "center",
                marginTop = 8,
                color = "#1C1917"
              )
            )
          )
        )
      )
    )
  )
)
```